version: 2
updates:
  # The project has no runtime dependencies; the only thing to keep current is
  # the set of actions the workflows call.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    commit-message:
      prefix: "ci"
    labels:
      - ci
